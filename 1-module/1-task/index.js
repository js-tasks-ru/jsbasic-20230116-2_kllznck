function factorial(n) {
  total = number;
  if (number >= 1) {
    for(number -= 1; number > 1; number--) {
      total *= number;
  }
  return total;
  } else if (number === 0) {
    return 1;
  }
  return 'can\'t calculate factorial';
}
